/**
 * Multiplayer / sector map layer reuses existing normalized tables (`systems`,
 * `planets`, `colonies`, `ships`, `users`) — no additional persistence in this
 * slice. Visibility projection lives in `features/multiplayer/presence.ts`.
 */
export {};
