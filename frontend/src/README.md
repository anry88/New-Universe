# `frontend/src` directory

This is the Telegram Mini App client. It is a Vite + React 18 + TypeScript project (`"type": "module"`) using Tailwind CSS, the Telegram Apps SDK, TanStack Query, Zustand, Pixi.js (for future canvas scenes), and Sentry. The current code is the minimum bootstrap; new pages, components, and state belong in the conventional subfolders described below.

## Layout

- `lib/` — shared infrastructure (API client, store, Sentry init, helpers).
  - **`analytics.ts`** — local-first frontend analytics emitter for task `P4-ANA-001`. It uses shared event definitions and property sanitization from `@shared/types/analytics`, emits `CustomEvent("nu:analytics")` for local checks, and only mirrors to PostHog when `VITE_POSTHOG_KEY` is configured.
  - **`analytics.test.ts`** — unit coverage for frontend envelope creation and unsafe property filtering.
  - **`api.ts`** — `apiFetch` wrapper for authenticated JSON calls; sends `Accept-Language` from the persisted UI locale and, after explicit session start, marks visible-tab requests with `X-NU-Online-Activity: 1` for backend play-time accounting. `openOnlineActivitySession()` opens or reopens the backend session baseline without counting the start call itself.
  - **`expedition-routing.ts`** — `buildExpeditionPreview()` wraps shared expedition route math for local and Jump Gate mission previews, including one-way colonizer fuel, one-way deployment fuel for combat/support/shield/missile hulls (`shipRole` feeds `isOneWayExpedition` for both local tactical points and Jump Gate routes), ETA, stored Jump Fuel requirements for one or two gate jumps, caller-supplied gate-leg route distance when the UI is plotting Home Gate -> destination Gate -> selected target point, and a Jump Fuel override for moving an already-stationed ship inside the same opened system.
  - **`expedition-routing.test.ts`** — unit coverage that keeps frontend Jump Gate scout/colonizer/combat preview math aligned with backend launch results.
  - **`i18n.tsx`** — React i18n provider/hook. Exports `I18nProvider`, `useI18n`, and `translate`; loads `locales/en.json` + `locales/ru.json`, persists locale changes, and performs simple `{param}` interpolation.
  - **`locale.ts`** — locale persistence/resolution helpers (`readStoredLocale`, `persistUiLocale`, `getUiLocale`, `normalizeUiLocale`) backed by `@shared/types/locale`.
  - **`locale.test.ts`** — unit coverage for locale normalization (`en` fallback, Telegram/Accept-Language `ru` detection).
  - **`production.ts`** — small UI helpers for recipe defaults, production-start eligibility, and localized block text from server preview responses.
  - **`production.test.ts`** — unit coverage for production helper behavior.
  - **`fleet.ts`** — `isCargoTransferShipType`, `isCargoTransferShip`, and `formatCargoTransferError` keep cargo-transfer entrypoints limited to logistics ships and map server cargo, fuel, Jump Fuel, and inter-system Logistics-gate failures to localized UI copy.
  - **`fleet.test.ts`** — unit coverage for cargo-ship filtering and cargo error localization keys.
  - **`jump-gate-destination.ts`** — converts known Jump Gate destination summaries into renderable public-system map snapshots and neutral localized display names for System Map and fleet/refuel deep-links.
  - **`timers.ts`** — `timerSnapshot()` and `formatTimerDuration()` for local countdown/progress rendering from server timestamps without polling `/me` every second.
  - **`timers.test.ts`** — fake-clock coverage for remaining-time, derived-start, and due-state progress math.
  - **`ship-queue.ts`** — `queueItemFromBuildingShip()` and `mergeShipBuildQueue()` keep building ships visible as queued even before `/ships/queue` refetches, so mission/cargo/jump actions stay blocked while construction is pending.
  - **`ship-queue.test.ts`** — unit coverage for building-ship fallback rows and authoritative `/ships/queue` rows winning over `/me` fallback rows.
  - **`ship-build-eligibility.ts`** — `resolveShipBuildBlockedReason(planet, shipType, researchRows?)` for local shipyard planner blocking on required building level, mirrored ship research gates, available build-cost resources, single build lane, and summed completed `spaceport` landing capacity; UI formatting uses localized entity labels instead of raw ids.
  - **`ship-build-eligibility.test.ts`** — unit coverage for cargo_light shipyard L2, Logistics L1, capital-resource build blockers, queued spaceports, and multi-spaceport capacity.
  - **`systemMapLayout.test.ts`** — Vitest coverage for shared system-map guide rings, including obfuscated `unknown` planets from `/me` not expanding the rendered orbit count, public-system planet order staying one body per orbit, and planet positions staying stable after display-name renames.
  - **`resourceBarScope.ts`** — `planetInventoryApiPath(planetId)` for `GET /resources/planets/:id` (resource top bar + inventory).
  - **`sectorMap.ts`** — sector-map UI helpers: `summarizeSectorEntities`, `sectorEntityKey`, `hasLiveSectorActivity`, and `sectorEntityDisplay` keep local/neutral/foreign counts, stable moving-ship keys, active-sector polling decisions, and visibility-safe detail labels out of the React/Pixi components.
  - **`sectorMap.test.ts`** — unit coverage for sector contact counts, stable moving-ship keys, live activity polling detection, and foreign-summary redaction so hidden ship/planet details are not displayed by the UI helper layer.
  - **`homeSystemTitle.ts`** — `formatHomeSystemTitleForUser()` builds localized home-system banners from `/me` (`@shared/format/homeSystemNaming`).
  - **`uiLocale.ts`** — compatibility re-export for `getUiLocale()` and `UiLocale`; new code should import from `locale.ts` / `i18n.tsx`.
  - **`sentry.ts`** — Sentry browser init.
  - **`tech-tree.ts`**, **`research-eligibility.ts`**, **`research-queue.ts`**, **`research-planet.ts`** — research UI helpers and tests. `research-queue.ts` identifies the single active research timer and blocks starts for other branches while that timer is running; `research-planet.ts` chooses the settled planet with the best ready lab so research costs are paid from the lab world instead of whatever planet is first in `/me`.
- `hooks/` — custom React hooks.
  - **`useAuth.ts`** — manages JWT session state in memory via Zustand, starts a backend online session after Telegram login, and only then enables `/me`.
  - **`useExpeditions.ts`** — `useLaunchExpedition()` wraps `POST /expeditions` and accepts the shared `LaunchExpeditionRequest`, including `routeMode='jump_gate'` with `destinationSystemId` and optional destination-system target coordinates for scout routes.
  - **`useJumpGateState.ts`** — `useJumpGateState()` fetches `GET /jump-gate/state` for mission UI destination lists, gate lock/calibration status, and known public-system body summaries; it keeps a slow refresh while idle and switches to short polling when destination planet building summaries report recent surface combat. `useRandomJump()` launches `POST /jump-gate/random-jump`, receives the gate-bound probe queue metadata, and refreshes gate/player state so the outbound route is visible before the destination opens.
  - **`useSystemTacticalState.ts`** — `useSystemTacticalState(systemId)` fetches `GET /systems/:systemId/tactical-state` for the currently rendered system only, keeping live self/foreign fleet-contact position, motion, status, HP, and combat data out of the broader Jump Gate destination list and out of `/me`. It keeps a slow selected-system refresh for new arrivals, switches to short-interval polling while any visible tactical contact is moving or has a recent combat tick, and keeps that polling alive across Telegram/WebView focus quirks.
  - **`useSystemTacticalState.test.ts`** — pure coverage for tactical-state polling intervals across idle, moving, and recent-combat contacts.
  - **`useResearch.ts`** — `useStartResearch()` wraps `POST /research/start` with optimistic `me` cache updates (`startedAt` + `completesAt` + in-flight branch), rollback on error, and invalidation on settle; `useRushResearch()` calls `POST /research/rush` and refreshes `/me`.
  - **`useShips.ts`** — `useBuildShip()` wraps `POST /ships/build` with the same optimistic queue pattern as research: writes a temporary building ship into `['me']` plus `['ship-queue']`, rolls back on error, replaces temp ids from the server queue metadata on success, and invalidates both caches on settle. `useShipQueue()` still handles due-time refresh, `useRushShip()` refreshes both caches after rush, `useRefuel()` posts routed `/ships/refuel` transfer orders, and `useRefuelReplenish()` posts `/ships/refuel/replenish` refill orders before invalidating `/me`.
  - **`useMe.ts`** — pure React Query hook for fetching current player data from `GET /me`; live selected-system movement/combat polling is owned by `useSystemTacticalState`, not by `/me`.
  - **`useMeCompletionRefresh.ts`** — singleton mounted from `AppContent` that schedules one visible-tab refresh at the nearest active building/research/ship/expedition completion timestamp while countdowns tick locally in UI.
  - **`useMeCompletionRefresh.test.ts`** — pure coverage for selecting the nearest active completion timestamp and ignoring inactive queue rows.
  - **`useColonies.ts`** — manages the collection of player-owned/settled planets and tracks the focal planet across the UI via a dedicated Zustand store (`useColoniesStore`); excludes undiscovered bodies and any planet without an explicit `isColonized === true` flag so Home/Colonies show only buildable planets and never leak discovered-but-unsettled bodies into the home rail.
  - **`useMonetization.ts`** — React Query hooks for Telegram Stars monetization: `useStarsDiamondPacks()` reads `GET /monetization/stars/packs`, `useCreateStarsInvoice()` posts `POST /monetization/stars/invoice` before the shop opens Telegram's invoice UI, and `useConfirmStarsCheckout()` posts `POST /monetization/stars/checkout-result` after Telegram returns `paid` or restores a fallback checkout so the UI can update `/me.diamonds` from an explicit delivery result.
  - **`useColonies.test.ts`** — store-level coverage for switching the focal planet id (home rail / colonies).
- `pages/` — page-level components routed by `react-router-dom`.
  - **`Home.tsx`** — main game screen with resource bar, planet view, build queue, and bottom tab bar.
  - **`Colonies.tsx`** — lists all owned planets with their resources and status, allowing focal planet switching and initiating cargo transfers; `?cargoOrigin=<planetId>&cargoShip=<shipId>` opens the cargo dialog directly from fleet logistics shortcuts with the clicked transport preselected, while `cargoTarget` / `cargoRoute=jump_gate` preselect Jump Gate destination cargo from the map surface.
  - **`Profile.tsx`** — player profile card with sector/system details, controlled-planet count based only on settled planets, an EN/RU language switch backed by `PATCH /me/preferences`, notification category toggles (buildings, ships, research, expeditions, discoveries, colonies, cargo, combat) persisted through the same preferences endpoint, and a `RESUME TUTORIAL` action when onboarding is not completed yet.
  - **`Shop.tsx`** — Telegram Stars diamond-pack shop. It displays the shared pack ladder, emits safe Stars analytics events, calls `POST /monetization/stars/invoice`, opens the Telegram invoice through `window.Telegram.WebApp.openInvoice`, persists the active checkout only after Telegram returns `paid` or when URL fallback has no callback support, tags persisted checkouts with their trusted source so legacy pre-callback entries are dropped, clears storage when Telegram returns a non-confirmable callback status, treats Telegram `pending` as a short non-persistent processing notice, restores fallback checkouts after Mini App return/reload, confirms paid checkouts through `POST /monetization/stars/checkout-result` with bounded automatic delivery polling, shows a prominent payment-sync notice while Telegram delivery is pending, updates the cached diamond balance from the confirmation response, and uses a Telegram Stars-style price badge with a browser fallback for local development.
  - `onboarding/` — first-session onboarding tutorial pages. See [`pages/onboarding/README.md`](./pages/onboarding/README.md).
    - **`Onboarding.tsx`** — 5-step tutorial overlay (welcome → mine → storage → scout → send scout); syncs `POST /tutorial/sync` once when opened, claims 100-diamond step rewards through `POST /tutorial/claim`, and wires **Continue** to dismiss full-screen gate via App/sessionStorage so Home stays playable without redirect loops.
  - **`PlanetDetail.tsx`** — detailed planet view with building slots, upgrade options, post-build extractor resource switching, production entry points for manual processors, and server-sourced planet energy status; derives construction eligibility from `GET /buildings/types` limits plus live `/me` planets/research/deposit richness via `@shared/types/building-eligibility`, computes extractor resource choices and used/limit counts from `PlanetResource.richness` plus `Building.selectedResourceId`, starts builds/upgrades with optimistic `queueStartedAt` / `queueCompletesAt` then replaces temporary ids from server `queueItem` metadata, ignores buildings still in the initial queue for dependency previews, blocks non-Command-Center upgrades above the local Command Center level, keeps merely discovered planets read-only until `/me` marks them `isColonized`, keeps the planet rail scoped to colonized settlements unless the current detail page is an unsettled survey body, and consumes the `?slot=` deep-link param exactly once (stripping it from the URL after opening the dialog) so subsequent `/me` refetches do not reopen the build/upgrade modal after the player closes it.
  - **`SystemMap.tsx`** — page component for the interactive system map; treats only viewer-owned `isColonized` planets as openable settlements, keeps foreign colonized public planets as red hostile-colony targets without a colonizer CTA, keeps the Home system available in the Sector selector, persists the last selected Galaxy system so returning to the tab restores the previous Home/public-system view, lists opened public systems from `/jump-gate/state`, and renders selected destinations with one orbit ring per server-reported planet while hiding actual bodies until scouts or active colonies reveal them. Opened public systems use neutral EN/RU names, discovered or occupied public planets carry mineable resource summaries, standing building count, recent surface-combat state, and explicit orbit order; random-discovery limit failures are localized, and self/foreign tactical fleet contacts from `/systems/:systemId/tactical-state` render as the authoritative selected-system combat frame with hull silhouettes, stable HP bars, tap-to-open ship stat cards with fleet-style launch/cargo/jump/refuel actions for own ships, refueler support-reserve balances, and weapon-colored combat projectiles. `/me` Jump Gate ship markers are suppressed once tactical-state renders the same ship, but route trails stay visible so the player's plotted path does not vanish in shared systems. The outer-orbit Jump Gate panel still shows locked/unlocked state, Random jump launch, known-destination badges, and route action deep-links.
  - **`SectorMap.tsx`** — Phase 3 sector map: queries `GET /multiplayer/systems` for Home/discovered/colony/fleet anchor chips, then queries `GET /multiplayer/sectors/:sx/:sy/:sz/presence` for the chosen sector with faster polling while moving or recently fighting fleet contacts are present; supports manual sector coordinates, relation counters, and a visibility-safe contact detail panel backed by `SectorRenderer`.
  - **`Ships.tsx`** — fleet management screen with two tabs: roster (`/ships`) and shipyard build queue/planner (`/ships?tab=shipyard`) wired to optimistic `POST /ships/build`. The fleet roster row is a collapsible card that shows icon, name, location/ETA (with a recent-combat state when applicable), ordinary fuel, and Jump Fuel in the header, then reveals HP, shield status, build progress, and the send-mission/refuel buttons after tap; the per-ship description and combat-role/attack-state debug labels are intentionally not rendered on the fleet screen anymore. Shipyard rows now use the same collapsible pattern: the header shows icon, localized name, class, build time, and ready/locked state, while the expanded body shows description, cost, requirements, shield status when relevant, localized blockers, and the build action without HP/DPS/range/fuel meta columns. Shipyard also warns before colonizer construction when the current `/me.colonization` cap is already reached, keeps ships in `building` from falling back to idle while `/ships/queue` is loading, shows ETA/progress from merged `/me` + `/ships/queue` state, lets `stationed` Jump Gate point deployments open the mission dialog again instead of staying permanently deployed, exposes rush completion through the fixed bottom `QueueStrip` via `POST /ships/rush`, routes logistics ships into the Colonies cargo-transfer dialog with `cargoShip` preselection, routes Recon Probes to the Jump Gate panel, and honors `route=jump_gate&destinationSystemId=<id>` deep-links from the Jump Gate destination panel.
- **`Research.tsx`** — tech-tree screen (Cosmic Atlas): eight branches × **five** tiers aligned with `@shared/config/researchCatalog` (including Energy and Weapons), branch blurbs, localized applied-effects summary, per-tier `TechTreeNode` states (completed / in-progress timer / next pending / locked), tier detail sheet with lab/prereq/resource/queue blocking (BuildDialog-style), planet selection based on the best ready lab, optimistic start via `useStartResearch`, and active-tier diamond rush via `useRushResearch`.
- `components/` — reusable presentational components.
  - `pixi/` — canvas-based rendering components using PixiJS.
  - **`SystemRenderer.tsx`** — top-down system map renderer. Handles orbits, planets, star, Jump Gate, own ship markers, foreign ship contacts, HP bars, tap-to-open ship stat cards, and weapon-colored combat projectile overlays with pan/zoom logic, including palettes for toxic, metallic, and energetic anomalous common worlds.
    - **`SectorRenderer.tsx`** — Pixi sector radar for multiplayer markers. Draws the sector grid/background, relation colors, entity-type marker shapes, hit targets, spread offsets for co-located contacts, selected-contact rings, redacted `?` markers for unknown foreign summary entities, and movement-facing fleet silhouettes with a small engine flare when the backend provides a motion vector.
  - **`ResourceBar.tsx`** — planet-scoped top chips (`planetInventoryApiPath` when `planetId` is set), once-per-second visible-tab resource regeneration, **`user.diamonds`** on `CosmicTopBar`, priority ordering for iron/silicon/carbon before secondary resources, optional `planetLabel`, and an in-row **All** action opening `ResourceInventoryDrawer`. Fixed-planet inventory refetches only on planet changes and purchases instead of every `/me` update. Clicking a resource chip opens diamond purchase flow (`POST /resources/buy-with-diamonds`) for the current planet; successful purchases update the visible stockpile and diamond cache immediately, then refetch `/me` and the planet inventory in the background. Clicking the diamond chip opens `/shop` for Telegram Stars packs.
  - **`ResourceInventoryDrawer.tsx`** — full planet stockpile list (sorted by label) plus account-wide diamonds in **Account (global)** when `/me` returns `user.diamonds`; each resource row has `Buy with diamonds` action using the same purchase flow as top chips.
  - **`ResourceDiamondPurchaseDialog.tsx`** — right-panel purchase sheet for resource amount input + buy confirmation; shared entrypoint for top-bar and inventory clicks.
  - **`cosmic/resources.ts`** — resource id -> Cosmic Atlas icon/label dictionary plus reusable, wrap-safe resource amount badges used in cosmic UI; aligned with seeded resource ids (`iron`, `silicon`, `oil`, `tritium`, `antimatter`, `jump_fuel`, Silver Steel, C/SiC Composite, etc.) so resource chips, costs, yields, fuel, and production rows render icons instead of chemistry abbreviations and fall back through `@shared/types/entity-labels` for safe labels.
  - **`cosmic/resources.test.ts`** — Vitest coverage that every gameplay resource label has a Cosmic Atlas icon entry, resource glyph fallbacks are not alphanumeric abbreviations, and the starter military resources keep their real-material display names.
  - **`cosmic/planets.tsx`** — shared Cosmic Atlas planet portraits and biome metadata, including localized toxic, metallic, and energetic anomalous common-world variants.
  - **`cosmic/ships.tsx`** — shared Cosmic Atlas ship icon resolver used by the orbital map, fleet roster, shipyard planner, expedition dialog, and cargo dialog. It defines hull icons for active ship catalog ids, including `recon_probe` as the one-use Recon Probe for random Jump Gate discovery plus medium/heavy fighter, bomber, laser, shield, and rocket-carrier combat lines; placeholder labels use localized/humanized text instead of raw ids.
  - **`cosmic/ships.test.ts`** — Vitest coverage for active ship icon resolution, advanced combat / shield / rocket-carrier hull resolution, visible `recon_probe` lists, lifecycle tones, and unknown-hull fallback behavior.
  - **`PlanetView.tsx`** — focal planet portrait + slot grid; `PlanetRail` updates the focal planet via `setFocalPlanetId` (keeps the user on Home while syncing the top resource bar).
  - **`cosmic/buildings.tsx`** — Cosmic Atlas building icons keyed by backend catalog ids (including `battery`, `solar_plant`, `wind_turbine`, `fuel_generator`, `atomic_reactor`, `oil_pump`, `biomass_harvester`, and `refinery`); exports `resolveBuildingType(typeId)` (canonical ids plus legacy synonyms such as `laboratory` and alternate lab spellings matched via `/^research[_-]?lab$/i`) plus stable build-list category helpers (`energy`, `extraction`, `processing`, `logistics`, `shipbuilding`, `progress`, `special`). `cryo_factory` is mapped to the `processing` section with other production buildings, and unknown placeholders humanize ids for display instead of leaking slugs.
  - **`cosmic/buildings.test.ts`** — Vitest coverage for `resolveBuildingType` (known id resolution plus unknown-id fallback to the safe default icon).
  - **`cosmic/SystemMap.tsx`** — exports `CosmicSystemRenderer` (SVG orbital system map; pan/pinch/zoom) and optional `expeditionPick` for mission UI: tap-to-aim a flat sector ΔXY route point, pick an arbitrary destination-system point for Jump Gate scout/combat routes, colonizer target selection on discovered planets, draft trails from `launchPlanetId` or a fixed Jump Gate point, active expedition trails filtered to in-flight/returning rows so parked `stationed` point deployments do not keep stale route traces, orbit guide rings driven by visible bodies or an explicit destination `minimumOrbitCount`, a clickable outer-orbit or fixed destination Jump Gate marker, selectable shared Cosmic ship icons parked on their planet sprite, interpolated along routes, or pinned at local stationed tactical points, red hostile-colony/recent-surface-combat rings for foreign occupied planets under bombardment, and authoritative self/foreign tactical contacts rendered from `/systems/:systemId/tactical-state` with hull icons, stable HP bars, ship/contact stat cards, own-ship action callbacks, compact override cards for focused flows such as refuel target picking, stale `/me` Jump Gate marker suppression only after the same tactical contact is visible while keeping the route trail visible, weapon-specific projectile effects with kinetic weapons drawn as separate tracer rounds instead of laser-like beams, rendered contact spread points for overlapping ships, and tactical contact crossfire when visible contacts are fighting without an own `/me` marker. Public destination bodies keep generator orbit order so one body occupies one orbit even when anomalies are inserted. Static map layers and CSS route lines are memoized so route-heavy maps and expedition aim drags avoid repainting every orbit/planet each movement tick, while interactive map buttons bypass drag capture so taps still open cards/actions. The selected-planet card now also shows mineable deposits, hostile building count, replaces the colonizer action with a bombardment warning on foreign colonies, and can be dismissed by tapping free space.
  - **`cosmic/SystemMap.test.ts`** — Vitest coverage for system-map expedition trail projection, local stationed tactical points, tactical contact filtering, route-trail retention when tactical contacts render, tactical Jump Gate ship ownership, motion heading, weapon visual mapping, and self/foreign projectile crossfire including overlapping close-range combat.
  - **`BuildQueue.tsx`** — displays the current build queue head from `/me` / React Query cache so optimistic construction appears immediately; countdown timers and progress use server-derived or optimistic `queueStartedAt`, live rush cost uses `/me.rushPricing` via `@shared/types/diamonds` (`estimateRushDiamondCost`), and **`POST /buildings/rush`** stays disabled for temporary optimistic ids until the server confirms the real queue row.
  - **`ShieldStatus.tsx`** — compact shield hook/status presenter for fleet and shipyard rows. It displays shield HP/capacity, coverage radius, recharge rate, post-damage delay, and downtime using localized EN/RU text from `locales/`.
  - **`RefuelDialog.tsx`** — full-screen localized map flow for `refueler` orders. Transfer mode picks an idle docked own target ship (civilian or military) on the rendered system map, disables ordinary planet info cards while targeting ships, clamps ordinary fuel / Jump Fuel sliders by support fuel cargo, launch-planet stockpile after route costs, and the target ship's remaining tank capacity, then starts a routed `/ships/refuel` expedition. Replenish mode picks an owned planet on the map using the compact planet card and starts `/ships/refuel/replenish` so the refueler flies there before filling its own tanks and support fuel cargo. The dialog has an explicit local/Jump Gate route selector, renders known destination systems from `/jump-gate/state`, previews Home Gate -> destination Gate route costs, and passes `routeMode='jump_gate'` / `destinationSystemId` through to the backend; fleet deep-links only preselect the same visible route controls.
  - **`ResearchQueue.tsx`** — fixed bottom research queue strip for the one active research timer. It reuses Cosmic `QueueStrip`, shows localized queue-lock status, computes live rush cost from shared diamond pricing, and calls `POST /research/rush`.
  - **`BuildingSlot.tsx`** — wraps the Cosmic `BuildSlot` atom for planet infrastructure slots, including queue progress, server-derived energy disabled state, battery charge display, and current queued/paused production-process chips from `/me`.
  - **`BuildDialog.tsx`** — Cosmic Atlas bottom sheet for picking a building type on an empty slot; groups options into localized category sections, sorts building options inside those sections by unlock progression (dependency gate/depth), applies dashed/low-opacity styling when `blockedReasonFor` reports a shared `BuildBlockedReason`, keeps the sheet header compact, moves current energy balance and stored battery charge into the `battery` option context, still shows per-option projected net after build, uses shared planet energy formulas for solar/wind output previews, treats processor energy as active-process demand rather than idle drain, suppresses operational demand on energy-anomaly worlds, offers resource-choice chips for extractor buildings with `used/limit` deposit counts, shows the resource-specific extraction yield from shared balance helpers, opens an inline hint (`build-block-reason`) on tap, and only calls `POST /buildings/build` when the row is eligible.
  - **`UpgradeDialog.tsx`** — Cosmic Atlas bottom sheet for completed buildings. It shows shared L10 / Command Center cap blockers, level-up costs from `@shared/config/buildingUpgradeEconomy` including L6+ extra materials, resource-specific extractor yield previews, planet-scaled solar/wind output deltas, shipyard/production entry points, saved extractor target, battery-only planet energy state, energy-anomaly operational-demand suppression, and `selectedResourceId` switching through `POST /buildings/resource` with the same deposit-limit chips and localized block reasons used during construction.
  - **`CargoTransferDialog.tsx`** — interplanetary logistics interface for moving resources between owned settlements through the shared `CargoTransferRequest` contract; inter-system targets can opt into `routeMode='jump_gate'`, initial ship/target/route props support fleet and Jump Gate panel deep-links, resource rows use localized Cosmic resource names, call `/cargo/transfer/preview` for server-side ETA / fuel / Jump Fuel costs, then block launches when the origin planet cannot pay those route costs.
  - **`ExpeditionDialog.tsx`** — mission launch on a **single** Galaxy map: `CosmicSystemRenderer` with `expeditionPick` for local home-system routes, a Jump Gate route selector for known public destinations plus safe planet summaries from `/jump-gate/state` and tactical fleet contacts from `/systems/:systemId/tactical-state`, closed destination-system point targeting for scouts and military/support hulls by tapping an opened system map whose orbit count matches the hidden planet count and whose bodies keep one-per-orbit generator order, Jump Gate colonizer target selection by tapping a discovered resource-bearing planet on the destination map, shared ETA/fuel preview for local and gate routes using launch planet -> Home Gate -> destination Gate -> target point/planet distance, stationed-origin previews that draw point-to-point movement inside the same public system and charge only ordinary tank fuel, localized fuel labels, ordinary/Jump Fuel sliders whose minimum floats to the amount required for the selected mission while allowing extra tank fill up to capacity, one-way colonizer/combat Jump Gate deployment with `/colonies/eligibility` preflight warnings for cooldown and colony-cap blockers where relevant, localized launch errors, and no manual ΔZ input.
  - **`ProductionDialog.tsx`** — compact production-process sheet for buildings with recipes. It fetches `/resources/production/recipes`, previews selected output quantity via `/resources/production/preview`, shows required material inputs, duration, active/max production slots, per-slot energy demand, and current queued/paused process countdowns, confirms material spend, and starts processes through `/resources/production/start`.
  - **`RequirementList.tsx`** — compact list of missing `{ branch, level }` research prerequisites for gated UI actions; uses localized research branch labels from `@shared/types/research`.
  - **`TechTreeNode.tsx`** — single-tier chip for Cosmic `tech-node` styles: completed/active countdown/pending/locked visuals using `timerSnapshot()` so progress can use server `startedAt`.
  - **`cosmic/atoms.tsx`** — shared Cosmic Atlas atoms including `BuildSlot` and `QueueStrip`; `BuildSlot` renders queue progress, energy/battery badges, and current production-process chips, while the strip supports long-duration ETA formatting, optional status subtitle copy, custom rush labels, and temporary-id disabled rush states for building, research, and ship queues.
  - **`Tutorial.tsx`** — reusable full-screen onboarding overlay with step list, per-step diamond reward blurbs from `@shared/config/tutorialRewards`, per-step claim buttons, **Continue** (back to game without skip), **Skip for now**, and **Back to game** when all five rewards are claimed.
- `locales/` — flat EN/RU dictionaries used by `lib/i18n.tsx`; see [`locales/README.md`](./locales/README.md).
- `assets/` — static assets imported by Vite (currently empty). Public brand assets that should be addressable by URL live outside `src` in `frontend/public/brand/`.

The folders above are reserved by `AGENTS.md` (`Engineering Rules` → "Keep frontend state and API calls in clear `lib/`, `pages/`, and `components/` boundaries"). Create them as soon as a feature needs them and document new files here.

## Top-level files

- **`main.tsx`** — Vite entry point. Order of operations:
  1. Imports `./lib/sentry` first so Sentry can capture errors thrown by later imports.
  2. Imports React and `react-dom/client`.
  3. Calls `init()` from `@telegram-apps/sdk-react` to initialize the SDK; a plain-browser failure is caught and logged so Sentry can report the page instead of losing the whole boot.
  4. Mounts the SDK pieces conditionally (`miniApp.mount`, `themeParams.mount`, `viewport.mount`). Each `*.mount.isAvailable()` guard skips mounting on platforms that do not support that piece. The `viewport.mount()` call is async; its rejection is caught and logged to keep the rest of the boot sequence alive.
  5. Imports `mockEnv.ts` so the dev environment is patched before `App` renders.
  6. Calls `miniApp.ready()` to tell the Telegram client that the Mini App finished loading.
  7. Renders `<App />` into `#root` inside `React.StrictMode`.
- **`App.tsx`** — application shell. Wraps React Query, `I18nProvider`, and `BrowserRouter`, runs Telegram login, waits for the first `/me` player snapshot before opening game routes, syncs the active locale from `/auth/telegram` or `/me`, guards first-session onboarding, reopens the online-session baseline when the document returns to visible, and lazy-loads route pages.
- **`mockEnv.ts`** — runs in local `DEV` and in E2E preview mode (`VITE_E2E_MOCK_TELEGRAM=1`). Calls `retrieveLaunchParams()`; if it throws (i.e. we are running in a plain browser without Telegram), it constructs a deterministic fake `initDataRaw` and theme via `mockTelegramEnv` so the SDK behaves as if it were inside Telegram. The mocked user (`Andrew Rogue`, `id: 99281932`) is a stable fixture; do not commit additional users without coordinating with the auth-test fixtures.
- **`index.css`** — Tailwind base/components/utilities entry imported by `main.tsx`; keeps modal backdrops/sheets (`.bd-backdrop` / `.bd-sheet`) above the fixed bottom nav so research/build dialogs remain clickable on mobile, makes bottom-nav items fill equal-width columns from the rendered tab count, and includes grouped build-dialog category headers plus the responsive sector-map control/detail panels.
- **`dummy.test.ts`** — a Vitest sanity test that asserts a trivial expression. Replace with real tests as features land.
- **`../tests/e2e/onboarding.spec.ts`** — Playwright E2E for the onboarding flow (open TMA mock env, complete/skip tutorial, build first mine, assert resource tick-up in UI).
- **`../tests/e2e/first-day.spec.ts`** — Playwright E2E for the Cosmic Atlas first-day flow using stable `data-testid` selectors (`cosmic-topbar`, `planet-portrait`, `planet-rail`, `slot-{idx}`, `queue-strip`, `bnav-{id}`) across Home → PlanetDetail → BuildDialog → QueueStrip → Tech/Fleet/Galaxy navigation.
- **`../tests/e2e/cargo.spec.ts`** — Playwright E2E regression for the fleet **Open Cargo** shortcut. It mocks `/me`, `/ships/types`, and inventory APIs, then asserts the Colonies cargo dialog opens with the clicked transporter preselected and localized resource names visible.
- **`../tests/e2e/system-map-combat.spec.ts`** — Playwright E2E regression for the Galaxy map combat render path. It uses Telegram mock auth plus API route mocks to assert local stationed combat deployments, Jump Gate route fallback markers/trails before tactical contact handoff, and visible kinetic tracer DOM.

## `lib/`

- **`tech-tree.ts`** — `TECH_TREE_DATA` (levels **1–5** per branch, costs/times/descriptions/effects, including Energy and Weapons) and `BRANCHES` metadata imported from `@shared/config/researchCatalog` (same source as `backend/src/config/research-catalog.ts`).
- **`tech-tree.test.ts`** — asserts all research branches × five tiers stay aligned with `RESEARCH_MAX_LEVEL` for epic **P2-EPIC-RESEARCH** UI coverage.
- **`research-eligibility.ts`** — `evaluateResearchEligibility` mirrors `/research/start` lab + prerequisite checks for UI lock copy; optional `planetResources` adds resource-shortage messaging aligned with server deductions.
- **`research-planet.ts`** — `readyLabLevel()` and `selectResearchPlanet()` choose the settled planet that has the highest ready lab for the Research page, falling back to the first settled planet only when no lab is available so the UI can still show lab blockers.
- **`research-queue.ts`** — `getActiveResearch()` and `researchStartBlockedByActive()` implement the frontend one-active-research queue rule.
- **`research-queue.test.ts`** — Vitest coverage for active-timer detection, due-row ignore behavior, and blocking another branch while one timer is active.
- **`fleet.ts`** — cargo/fleet helper module. `isCargoTransferShipType()` and `isCargoTransferShip()` keep only logistics ships in cargo-transfer flows, while `formatCargoTransferError()` converts known server cargo, fuel, Jump Fuel, Logistics-gate, and known-common-system errors into localized dictionary keys.
- **`fleet.test.ts`** — Vitest coverage for logistics-only cargo filtering and mapped cargo-transfer server errors.
- **`jump-gate-destination.ts`** — converts `JumpGateKnownDestinationSummary` rows from `/jump-gate/state` into renderable `HomeSystem` snapshots and neutral localized display names for opened public-system maps shared by System Map and fleet/refuel flows.
- **`ship-queue.ts`** — merges `/ships/queue` rows with `/me.ships` fallback rows so a `building` ship is never treated as idle during queue refetch gaps.
- **`ship-queue.test.ts`** — regression coverage for the building-ship fallback and authoritative queue-row precedence.
- **`ship-build-eligibility.ts`** — local planner helper that returns the first ship build blocker (`missingBuilding`, `missingResearch`, `insufficientResource`, `queueFull`, or `spaceportCapacityFull`) for localized shipyard rows with entity labels instead of raw ids. `estimateLandingSlotUsage()` mirrors the backend's `loadLandingSlotUsage` against the `/me` snapshot to compute summed completed `spaceport` capacity vs docked ships and active expedition reservations.
- **`ship-build-eligibility.test.ts`** — Vitest coverage for lightweight-transporter L2 shipyard, Logistics L1, resource availability, single-lane shipyard queue, single/multiple spaceport capacity, queued spaceport, and landing-slot reservations.
- **`production.ts`** — `defaultProductionRecipeId`, `canStartProduction`, and `productionBlockedText` keep production dialog controls deterministic and translate structured server block reasons, including full production-slot blockers returned by preview.
- **`production.test.ts`** — Vitest coverage for production helper selection, disabled state, and localized block messages.
- **`planet-energy.test.ts`** — Vitest coverage for shared frontend energy previews, especially wind-turbine planet-size scaling in build/upgrade sheets.
- **`build-eligibility.ts`** — `resolveQueueFullBlockedReason` and `resolveInsufficientResourcesBlockedReason` mirror the backend's single-lane build queue and `spendResources` cost check so BuildDialog/UpgradeDialog can refuse a doomed request before it reaches the server.
- **`build-eligibility.test.ts`** — Vitest coverage for queue-full detection, missing-resource enumeration, zero-cost short-circuit, and empty planet inventory.
- **`timers.ts`** — pure local timer helpers for countdown/progress snapshots and compact duration labels. Network synchronization stays in hooks (`useMe`, `useShipQueue`) and fires at due timestamps, not every UI tick.
- **`timers.test.ts`** — Vitest coverage for exact timestamp progress, derived start fallback, and due-state clamping.
- **`api.ts`** — Unified fetch client. Automatically injects `X-Telegram-Init-Data` from the SDK, `Authorization: Bearer <token>` when a session is active, and `Accept-Language` from the persisted UI locale. After `useAuth` opens an online session, visible-tab requests also carry `X-NU-Online-Activity: 1`; hidden-tab/background refetches omit it, and returning to visible opens a fresh baseline so worker-driven completion sync does not inflate play time.
- **`analytics.ts`** — Shared browser analytics helper. `trackFrontendEvent()` records `client_session_started`, `session_authenticated`, and routed `page_viewed` events from the app shell/auth flow, filters unsafe Telegram/auth/user properties through the shared taxonomy, dispatches `nu:analytics` for local verification, and keeps external PostHog capture disabled until `VITE_POSTHOG_KEY` is present.
- **`i18n.tsx`** — React locale context/provider over the EN/RU JSON dictionaries, with persisted locale switching and `t(key, params?)`.
- **`locale.ts`** — reads/writes the persisted UI locale (`nu_preferred_locale`, plus legacy `ui_locale`) and normalizes Telegram/header values through the shared locale contract.
- **`locale.test.ts`** — Vitest coverage for `normalizeLocale`.
- **`telegramRuntime.ts`** — runtime guard for Telegram-only production boot. It allows dev/E2E mock environments, but production requires readable Telegram `initDataRaw` before the app starts login.
- **`telegramRuntime.test.ts`** — Vitest coverage for mock-runtime allowance, valid launch params, and plain-browser blocking.
- **`stars-checkout.ts`** — pure Telegram Stars checkout UI helpers: localization-key mapping, status tone selection, paid invoice status confirmation, Telegram pending callback normalization, transient notice clearing, source-tagged active-checkout serialization/restoration, and bounded auto-confirm polling gates for delayed Telegram delivery confirmations, including Mini App invoice URL fallback states that only report `pending`.
- **`stars-checkout.test.ts`** — Vitest coverage for checkout status key/tone mapping, transient notice clearing, source-tagged storage, and the pending/pending-delivery auto-confirm guard.
- **`sectorMap.ts`** — Pure sector-map presentation helpers. `summarizeSectorEntities()` powers local/neutral/foreign counters, `sectorEntityKey()` gives React/Pixi a stable contact key even as moving ships update coordinates, `hasLiveSectorActivity()` drives active-sector polling, and `sectorEntityDisplay()` redacts unknown foreign summary contacts before labels/details are rendered.
- **`sectorMap.test.ts`** — Vitest coverage for sector counters, moving-ship key stability, active polling detection, and visibility-safe foreign contact labels.
- **`sentry.ts`** — initializes `@sentry/react` only when `import.meta.env.VITE_SENTRY_DSN` is present.
  Uses `browserTracingIntegration` and `replayIntegration` with sample rates from `VITE_SENTRY_TRACES_SAMPLE_RATE`, `VITE_SENTRY_REPLAY_SESSION_SAMPLE_RATE`, and `VITE_SENTRY_REPLAY_ERROR_SAMPLE_RATE`; production defaults are `0.1`, `0.01`, and `1.0`, while non-production keeps higher local sampling. The module exports the `Sentry` namespace so error-boundary or `Sentry.captureException` calls can import directly from here.

## Adding a feature to the frontend

1. Place fetch and persistence helpers in `frontend/src/lib/` (TanStack Query hooks, Zustand stores, fetcher functions).
2. Place page-level components in `frontend/src/pages/` and presentational components in `frontend/src/components/`.
3. Add the route to `App.tsx` (or `pages/App.tsx` once it is moved out) using `react-router-dom`.
4. If the feature talks to the backend, define the request/response types in [`shared/types/`](../../shared/README.md) and import them on both ends.
5. Add a Vitest test for any non-trivial logic and update this README to describe the new file.

## Verification commands

`frontend/package.json` defines:

- `npm run dev` — Vite dev server on port `5173` (Compose passes `--host 0.0.0.0`).
- `npm run build` — `tsc && vite build`.
- `npm run preview` — Vite preview of the production build.
- `npm run lint` — ESLint over `*.ts,*.tsx`.
- `npm test` — Vitest run.
- `npm run test:e2e` — Playwright end-to-end run (`frontend/playwright.config.ts`, tests under `frontend/tests/e2e/`). On GitHub, Playwright runs from `.github/workflows/e2e.yml` only after manual dispatch or when the PR has label **`run-e2e`**, not from the default `ci.yml`; locally use `RUN_PLAYWRIGHT_E2E=1 ./scripts/ci-verify.sh` for the same combined gate.
